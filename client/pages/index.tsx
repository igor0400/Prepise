import type { NextPage } from 'next';
import PageWrapper from '../hoks/PageWrapper';

const Home: NextPage = () => {
   return (
      <PageWrapper>
         Главная
      </PageWrapper>
   );
};

export default Home;
