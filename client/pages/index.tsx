import type { NextPage } from 'next';
import PageWrapper from '../hoks/PageWrapper';

const Home: NextPage = () => {
   return (
      <PageWrapper>
         <h1 className="text-3xl font-bold m-16">Prepise</h1>
      </PageWrapper>
   );
};

export default Home;
